apiVersion: apps/v1
kind: Deployment
metadata:
  name: zzv-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zzv-nginx
  template:
    metadata:
      labels:
        app: zzv-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-config
